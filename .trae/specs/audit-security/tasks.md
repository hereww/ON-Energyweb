# Tasks

- [ ] 任务 1: 清理硬编码凭据与敏感信息
  - [ ] 扫描所有源代码文件，确保无明文密码、Token 或私钥泄露。
  - [ ] 检查 `.env.example` 是否包含真实的敏感数据而非示例占位符。
- [ ] 任务 2: 加固种子脚本 (`src/scripts/seed.ts`)
  - [ ] 移除 `SEED_ADMIN_PASSWORD` 的默认回退值 `'ChangeMe123!'`。
  - [ ] 实现强制检查机制：若环境变量未提供，则停止执行并报错提示。
- [ ] 任务 3: 审查配置文件的安全性加载
  - [ ] 验证 `src/payload.config.ts` 中的 `secret` 和 `connectionString` 是否严格依赖环境变量且无不安全的回退逻辑。
- [ ] 任务 4: 执行依赖项安全性扫描
  - [ ] 运行 `npm audit` 或类似工具检查已知的高危漏洞。
  - [ ] 为发现的 P0/P1 级别漏洞提供更新建议或修复方案。

# Task Dependencies
- 任务 2 和 任务 3 之间无依赖，可并行执行。
- 所有任务在完成前应先通过任务 1 的扫描结果引导。
